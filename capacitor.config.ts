import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alabus-prototype-vuejs-ionic',
  webDir: 'dist'
};

export default config;
